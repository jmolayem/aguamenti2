<div class="container-fluid">
      
      <div class="row row-offcanvas row-offcanvas-left">
        
         <div class="col-sm-3 col-md-2 sidebar-offcanvas" id="sidebar" role="navigation">
           
            <ul class="nav nav-sidebar">
              <li class="active"><a href="#">Overview</a></li>
              <li><a href="#">Datasets</a></li>
              <li><a href="#">Friends</a></li>
            </ul>
            <ul class="nav nav-sidebar">
              <li><a href="">My Models</a></li>
              <li><a href="">My Vision</a></li>
              <li><a href="">My Language</a></li>
            </ul>
        </div><!--/span-->
        
        <div class="col-sm-9 col-md-10 main">
          
          <!--toggle sidebar button-->
          <p class="visible-xs">
            <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas"><i class="glyphicon glyphicon-chevron-left"></i></button>
          </p>
          
      <h1 class="page-header">
            Dashboard
          </h1>

          <div class="row placeholders">
            <div class="col-xs-6 col-sm-3 placeholder text-center">
              <h4>Label</h4>
              <span class="text-muted">Something else</span>
            </div>
            <div class="col-xs-6 col-sm-3 placeholder text-center">
              <h4>Label</h4>
              <span class="text-muted">Something else</span>
            </div>
            <div class="col-xs-6 col-sm-3 placeholder text-center">
              <h4>Label</h4>
              <span class="text-muted">Something else</span>
            </div>
            <div class="col-xs-6 col-sm-3 placeholder text-center">
              <h4>Label</h4>
              <span class="text-muted">Something else</span>
            </div>
          </div>
<hr>
<table class="table table-striped table-bordered table-hover">
  <tr>
    <th class="center">Purpose</th>
    <th class="center">Organization</th>
    <th class="center">Snippet</th>
    <th class="center">Full Dataset</th>
</tr>

  <% @datagalleries.each do |datagallery| %>
<tr>
            <td><%= image_tag datagallery.image.url(:thumb) %></td>
            <td><%= datagallery.name %></td>
            <td><%= datagallery.organization %></td>
            <td><%= link_to 'Dataset Snippet', @datagallery.snippet.url, class:"btn btn-primary"%></td>
            <td><%= link_to 'Full dataset', @datagallery.dataset.url, class:"btn btn-primary"%></td>
            <td>
              <div class="btn-group">
                <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                  Action <span class="caret"></span>
                </button>
              <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Use', datagallery%></li>
              <li><%= link_to 'Edit', edit_model_path(datagallery), class: "glyphicon glyphicon-pencil" %></li>
             <li><%= link_to 'Destroy', model, method: :delete, data: { confirm: 'Are you sure?' }, class: "glyphicon glyphicon-trash" %></li>
           </ul>
         </div>
              </td>
            </tr>
            <% end %>
          </table>
<br>
<% if user_signed_in? %>
<div class="center">
<%= link_to new_model_path, class: "btn btn-primary", data: { no_turbolink: true } do %>
  <i class="glyphicon glyphicon-plus"></i> New Brain
<% end %>
</div>

<% end %>
